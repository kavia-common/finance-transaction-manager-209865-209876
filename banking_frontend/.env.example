# Frontend environment variables for Remotion (Vite-style)
# Preferred: Use Vite-style variable name so it is exposed to the client build.
VITE_BACKEND_API_URL=http://localhost:3001

# Fallback (also supported by client): Not recommended for Vite exposure, but read as fallback.
BACKEND_API_URL=http://localhost:3001
